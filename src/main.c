/*
 * git-client — entry point.
 *
 * Parse argv and dispatch to commands under src/cli/commands/.
 */
#include <stdio.h>

int main(int argc, char **argv)
{
	(void)argc;
	(void)argv;
	puts("git-client (not yet implemented)");
	return 0;
}
